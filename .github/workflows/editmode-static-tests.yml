name: EditMode fake-Unity tests

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Run Doctor fixture and schema tests
        shell: powershell
        run: powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\run-tests.ps1

      - name: Run Baseline fake-Unity tests
        shell: powershell
        run: powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\unity-baseline-verification\run-tests.ps1

      - name: Run EditMode fake-Unity tests
        shell: powershell
        run: powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\unity-editmode-verification\run-tests.ps1

      - name: Verify checkout remained unchanged
        shell: powershell
        run: |
          git diff --exit-code
          $status = @(git status --short --untracked-files=all)
          if ($status.Count -ne 0) {
            $status | Write-Host
            throw "EditMode tests changed the checkout."
          }
