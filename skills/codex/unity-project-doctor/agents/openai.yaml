interface:
  display_name: "Unity Project Doctor"
  short_description: "Deterministic read-only Unity static audit"
  default_prompt: "Use $unity-project-doctor to audit the Unity project in the current working directory without changing it."

policy:
  allow_implicit_invocation: false
